# Лабораторная работа №3. Разработка простой темы WordPress

## Цель работы

Научиться создавать собственную тему WordPress, разобраться в её минимальной структуре и принципах работы шаблонов.

## Описание лабораторной работы

В рамках лабораторной работы была разработана простая пользовательская тема WordPress.  
Тема включает минимальный набор файлов, необходимых для работы WordPress, а также дополнительные шаблоны для отображения страниц, записей и архивов.

В ходе выполнения работы были изучены принципы работы системы шаблонов WordPress и структура темы.

## Инструкции по запуску проекта

1. Установить локальный сервер (например XAMPP, OpenServer или MAMP).
2. Установить WordPress.
3. Перейти в папку:

```

wp-content/themes

```

4. Скопировать папку темы:

```

usm-theme

```

5. Открыть административную панель WordPress:

```
http://localhost/wordpress/wp-admin

```

6. Перейти в раздел:

```

Appearance → Themes

```

7. Активировать тему **USM Theme**.

### Скриншот структуры файлов

![alt text](Image/image.png)

# Этапы выполнения лабораторной

## Шаг 1. Подготовка среды

В папке WordPress:

```

wp-content/themes

```

была создана директория темы:

```

usm-theme

```

В файле `wp-config.php` была включена отладка:

```php
define('WP_DEBUG', true);
```

## Шаг 2. Создание обязательных файлов темы

В папке темы были созданы файлы:

```
style.css
index.php
```

Файл `style.css` содержит метаданные темы и базовые стили.

Файл `index.php` используется как главный шаблон темы.

## Шаг 3. Общие части шаблонов

Для разделения структуры сайта были созданы файлы:

```
header.php
footer.php
```

Они подключаются в шаблонах с помощью функций WordPress:

```php
get_header();
get_footer();
```

На главной странице выводятся последние записи с помощью **WordPress Loop**.

### Скриншот главной страницы сайта

Сделать скриншот главной страницы сайта после активации темы.

## Шаг 4. Файл функций

В папке темы был создан файл:

```
functions.php
```

Он используется для подключения стилей темы.

```php
function usm_theme_styles(){
    wp_enqueue_style('usm-style', get_stylesheet_uri());
}

add_action('wp_enqueue_scripts', 'usm_theme_styles');
```

## Шаг 5. Дополнительные шаблоны

Были созданы файлы:

```
single.php
page.php
sidebar.php
comments.php
archive.php
```

Они используются для отображения различных типов страниц WordPress.

### Скриншот страницы записи

![alt text](Image/image-1.png)

## Шаг 6. Стилизация темы

В файл `style.css` были добавлены стили для следующих элементов:

- header
- footer
- основной контент
- sidebar

Для размещения элементов используется **Flexbox**.

## Шаг 7. Скриншот темы

В папку темы был добавлен файл:

```
screenshot.png
```

Размер изображения:

```
1200x900 px
```

![alt text](screenshot.png)

Этот файл используется WordPress для отображения превью темы.

### Скриншот темы в админ панели

```
Appearance → Themes
```

![alt text](Image/image-2.png)

## Шаг 8. Активация темы

В админ-панели WordPress была активирована тема:

```
Appearance → Themes → USM Theme
```

После активации сайт начал отображаться с использованием разработанной темы.

### Скриншот сайта

![alt text](image.png)

# Примеры использования темы

После активации темы WordPress автоматически использует различные шаблоны:

- **index.php** — главная страница
- **single.php** — страница записи
- **page.php** — страницы сайта
- **archive.php** — архивы записей

# Ответы на контрольные вопросы

### Какие два файла являются обязательными для любой темы WordPress?

Обязательные файлы:

```
style.css
index.php
```

### Как подключаются общие части шаблонов?

Через функции WordPress:

```php
get_header();
get_footer();
get_sidebar();
```

### Чем отличаются index.php, single.php и page.php?

- **index.php** — основной шаблон темы
- **single.php** — отображение одной записи
- **page.php** — отображение страниц сайта

### Зачем нужен файл functions.php в теме?

Файл `functions.php` используется для добавления функциональности темы:

- подключение CSS и JS
- регистрация меню
- регистрация виджетов
- добавление функций темы

# Использованные источники

1. [https://developer.wordpress.org/themes/](https://developer.wordpress.org/themes/)
2. [https://wordpress.org/documentation/](https://wordpress.org/documentation/)
3. [https://developer.wordpress.org/themes/basics/template-files/](https://developer.wordpress.org/themes/basics/template-files/)
